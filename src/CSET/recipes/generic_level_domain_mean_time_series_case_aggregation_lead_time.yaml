category: $LEVELTYPE Level Time Series
title: $MODEL_NAME $VARNAME $LEVEL domain mean time series aggregation by lead time
description: Plots a time series of the domain mean $VARNAME at $LEVELTYPE $LEVEL by averaging over all cases by using a consistent lead time.

steps:
  - operator: read.read_cubes
    constraint:
      operator: constraints.combine_constraints
      varname_constraint:
        operator: constraints.generate_var_constraint
        varname: $VARNAME
      level_constraint:
        operator: constraints.generate_level_constraint
        coordinate: $LEVELTYPE
        levels: $LEVEL

  - operator: collapse.collapse
    coordinate: [grid_latitude, grid_longitude]
    method: MEAN

  - operator: aggregate.ensure_aggregatable_across_cases

  - operator: collapse.collapse
    coordinate: "forecast_reference_time"
    method: MEAN

  # Make a single NetCDF with all the data inside it.
  - operator: write.write_cube_to_nc
    overwrite: True

  # Plot the data.
  - operator: plot.plot_line_series
    series_coordinate: forecast_period
