category: Daily Weather
title: $MODEL_NAME CURV spatial plot
description: |
  Generates the curvature (CURV) from MSLP and creates a spatila plot.
steps:
  - operator: read.read_cubes
    file_paths: $INPUT_PATHS
    model_names: $MODEL_NAME
    constraint:
        operator: constraints.combine_constraints
        varname_constraint:
            operator: constraints.generate_var_constraint
            varname: air_pressure_at_mean_sea_level
        cell_methods_constraint:
            operator: constraints.generate_cell_methods_constraint
            cell_methods: []
    subarea_type: $SUBAREA_TYPE
    subarea_extent: $SUBAREA_EXTENT

  - operator: curvature.global_curv_versiontwo
    radius: 1000
    num_radial_points: 8

  - operator: plot.spatial_pcolormesh_plot

  - operator: write.write_cube_to_nc
    overwrite: True
