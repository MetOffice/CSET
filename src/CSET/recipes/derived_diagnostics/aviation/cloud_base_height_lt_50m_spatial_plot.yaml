category: Aviation
title: $MODEL_NAME Cloud base height less than 50 m
description: |
  Generates a mask of whether cloud base is less than 50 m in a grid box and provides a
  spatial plot. A value of one implies cloud base is less than 50 m; a value of zero
  implies cloud base is not lower than 50 m (or is 50 m).

steps:
  - operator: read.read_cubes
    file_paths: $INPUT_PATHS
    model_names: $MODEL_NAME
    constraint:
      operator: constraints.combine_constraints
      varname_constraint:
        operator: constraints.generate_var_constraint
        varname: cloud_base_altitude
      cell_methods_constraint:
        operator: constraints.generate_cell_methods_constraint
        cell_methods: []
    subarea_type: $SUBAREA_TYPE
    subarea_extent: $SUBAREA_EXTENT

  - operator: misc.convert_units
    units: 'metre'

  - operator: filters.generate_mask
    condition: '<'
    value: 50

  - operator: plot.spatial_pcolormesh_plot

  - operator: write.write_cube_to_nc
    overwrite: True
