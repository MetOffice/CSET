category: Time series at Cardington
title: "Time series of latent heat flux at Cardington [52.10438N, -0.42286E]"
description: "Plots a time series of latent heat flux at gridpoint nearest to Cardington."

steps:
  - operator: read.read_cubes
    file_paths: $INPUT_PATHS
    model_names:
      - Obs (30min)
      - $MODEL_NAME
    constraint: ['$OBS_VARNAME', '$VARNAME']

  - operator: misc.mask_fill_values

  - operator: filters.filter_multiple_cubes

    Cardington:
      operator: constraints.combine_constraints
      varname_constraint:
        operator: constraints.generate_var_constraint
        varname: $VARNAME
      cell_methods_constraint:
        operator: constraints.generate_cell_methods_constraint
        cell_methods: []
        varname: $VARNAME
      height_constraint:
        operator: constraints.generate_attribute_constraint
        attribute: 'height'
        value: $HEIGHT

    $MODEL_NAME:
      operator: constraints.combine_constraints
      varname_constraint:
        operator: constraints.generate_var_constraint
        varname: $VARNAME
      cell_methods_constraint:
        operator: constraints.generate_cell_methods_constraint
        cell_methods: []
        varname: $VARNAME
      pressure_level_constraint:
            operator: constraints.generate_level_constraint
            levels: [ ]
            coordinate: "pressure"
      stash_constraint:
        operator: constraints.generate_attribute_constraint
        attribute: STASH

  - operator: fluxes.latent_heat_units

  - operator: misc.remove_attribute
    attribute: [ 'time_coverage_end', 'time_coverage_start', 'valid_min', 'valid_max', 'date', 'date_created' ]

  - operator: regrid.regrid_to_single_point
    lat_pt: 52.10438
    lon_pt: -0.42286
    latlon_in_type: "realworld"
    method: "Nearest"
    boundary_margin: 0

  # Make a single NetCDF with all the data inside it.
  - operator: write.write_cube_to_nc
    overwrite: True

  # Plot the data.
  - operator: plot.plot_line_series
