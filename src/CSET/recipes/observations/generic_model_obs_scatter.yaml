category: Point observation scatter plot
title: "Scatter $VARNAME $SUBAREA_NAME"
description: Extracts and plot scatter plot of $VARNAME.

steps:
  - operator: read.read_cubes
    file_paths: $INPUT_PATHS
    model_names: $MODEL_NAME
    subarea_type: $SUBAREA_TYPE
    subarea_extent: $SUBAREA_EXTENT
    constraint: ['observed_$VARNAME', '$VARNAME']

  - operator: filters.filter_multiple_cubes
    constraint:
        operator: constraints.generate_cell_methods_constraint
        cell_methods: []

  - operator: misc.extract_common_points
    coordinate: time

  - operator: misc.combine_cubes_into_cubelist
    first:
        operator: filters.filter_cubes
        constraint:
            operator: constraints.generate_var_constraint
            varname: observed_$VARNAME
    second:
       operator: regrid.interpolate_to_point_cube
       fld:
           operator: filters.filter_multiple_cubes
           constraint:
               operator: constraints.combine_constraints
               var_constraint:
                   operator: constraints.generate_var_constraint
                   varname: $VARNAME
       point_cube:
           operator: filters.filter_cubes
           constraint:
               operator: constraints.generate_var_constraint
               varname: observed_$VARNAME

  - operator: plot.plot_scatter_series
    sequence_coordinate: station        ## separate output for each station
      #sequence_coordinate: realization    ## all output on 1 plot

  - operator: write.write_cube_to_nc
    overwrite: True
