{% if EXTRACT_TRANSECT %}
[scheduling]
    [[graph]]
    P1 = """
    recipe_start => transect => recipe_finish
    """

{% for var in CS_VARS %}
[runtime]
    [[transect]]
    inherit = PARALLEL
    execution time limit = PT60M
        [[[environment]]]
        CSET_RECIPE_NAME = "transect.yaml"
        CSET_ADDOPTS = "--CS_STARTCOORDS={{CS_STARTCOORDS}} --CS_FINISHCOORDS={{CS_FINISHCOORDS}} --CS_VARS={{var}}"
{% endfor %}
{% endif %}
